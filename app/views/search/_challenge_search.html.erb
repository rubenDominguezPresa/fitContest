<a>
     <%=image_tag result.image.url(:medium), class:'img-responsive'%>
    </a>
    <div>
      <div class="content" style="width:120px">
         <%=link_to result.name, challenge_path(result.name) %>
      </div>
    </div>

      <%=link_to (image_tag result.image.url(:medium), class:'img-responsive'), challenge_path(challenge) %>